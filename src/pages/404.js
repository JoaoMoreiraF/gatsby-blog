import React from "react";
import Layout from "../components/Layout";

export default () => (
  <Layout>
    <p>
      <strong>Infelizmente a página desejada não pode ser encontrada!</strong>
    </p>
  </Layout>
);